import NiceModal from '@ebay/nice-modal-react';

const HelpModal = NiceModal.create(() => {
  return (
    <div>
      <h1>Help</h1>
      <p>Welcome, name!</p>
    </div>
  );
});

export default HelpModal;
